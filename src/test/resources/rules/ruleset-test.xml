<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:r="http://www.rulii.org/schema/rulii"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.rulii.org/schema/rulii
           https://www.rulii.org/spring/rulii-spring.xsd">

    <r:scripting defaultLanguage="el"/>

    <!-- ======================================================
         Standalone rules used as <bean-ref name="..."/> targets
    ====================================================== -->

    <r:rule name="AdultCheckRule" description="Passes when age >= 18">
        <r:given>#ctx.age >= 18</r:given>
    </r:rule>

    <r:rule name="PositiveCountRule" description="Passes when count > 0">
        <r:given>#ctx.count > 0</r:given>
    </r:rule>

    <!-- ======================================================
         Basic ruleset — references two existing rule beans.
    ====================================================== -->

    <r:ruleset name="BasicRuleSet" description="Wraps two standalone rules">
        <r:rules>
            <r:bean-ref name="AdultCheckRule"/>
            <r:bean-ref name="PositiveCountRule"/>
        </r:rules>
    </r:ruleset>

    <!-- ======================================================
         Ruleset with declared input parameters.
         - order  : required String
         - maxItems: optional Integer (no default value expression)
    ====================================================== -->

    <r:ruleset name="ParameterizedRuleSet" description="Demonstrates input parameter declarations">
        <r:param name="order" type="java.lang.String"/>
        <r:param name="maxItems" type="java.lang.Integer" required="false"/>
        <r:rules>
            <r:rule name="OrderNotEmptyRule">
                <r:given>#ctx.order.length() > 0</r:given>
            </r:rule>
        </r:rules>
    </r:ruleset>

    <!-- ======================================================
         Ruleset that exercises all lifecycle hooks:
         pre-condition, initializer, stop-condition, finalizer.
         Bindings expected: value (Integer), count (Integer)
    ====================================================== -->

    <r:ruleset name="LifecycleRuleSet" description="Exercises all lifecycle hooks">
        <r:pre-condition>#ctx.value > 0</r:pre-condition>
        <r:initializer>'init'</r:initializer>
        <r:rules>
            <r:rule name="LifecycleInlineRule">
                <r:given>#ctx.value > 5</r:given>
            </r:rule>
        </r:rules>
        <r:stop-condition>#ctx.count > 100</r:stop-condition>
        <r:finalizer>'fin'</r:finalizer>
    </r:ruleset>

    <!-- ======================================================
         Ruleset that declares BOTH validating="true" and a custom
         finalizer — the two must compose, not overwrite each other.
         Bindings expected: value (String), finalized (Boolean)
    ====================================================== -->

    <r:notNull name="ValueRequiredRule" binding="value"/>

    <r:ruleset name="ValidatingFinalizerRuleSet" description="validating and finalizer compose" validating="true">
        <r:rules>
            <r:bean-ref name="ValueRequiredRule"/>
        </r:rules>
        <r:finalizer>#ctx.setValue('finalized', true)</r:finalizer>
    </r:ruleset>

</beans>
