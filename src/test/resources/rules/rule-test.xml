<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:r="http://www.rulii.org/schema/rulii"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.rulii.org/schema/rulii
           http://www.rulii.org/spring/spring-rulii.xsd">

    <!-- Set SpEL as the default scripting language for all expressions in this file -->
    <r:scripting defaultLanguage="el"/>

    <!--
        Rule using inline text for the condition and both action branches.
        Bindings expected at runtime: age (Integer)
    -->
    <r:rule name="AgeCheckRule" description="Checks if a person is an adult">
        <r:given>#ctx.age >= 18</r:given>
        <r:then>'adult'</r:then>
        <r:otherwise>'minor'</r:otherwise>
    </r:rule>

    <!--
        Rule using the expr attribute instead of inline text.
        Bindings expected at runtime: value (Integer)
    -->
    <r:rule name="PositiveValueRule" description="Checks if the value is positive">
        <r:given expr="#ctx.value > 0"/>
    </r:rule>

    <!--
        Rule with a pre-condition guard.  When age &lt;= 0 the rule is skipped entirely
        (SKIPPED status) and the main condition is never evaluated.
        Bindings expected at runtime: age (Integer), value (Integer)
    -->
    <r:rule name="PreConditionRule" description="Has a pre-condition guard">
        <r:pre-condition>#ctx.age > 0</r:pre-condition>
        <r:given>#ctx.value > 10</r:given>
    </r:rule>

    <!--
        Rule with two then-actions (executed in declaration order).
        Bindings expected at runtime: count (Integer)
    -->
    <r:rule name="MultiActionRule" description="Has multiple then-actions">
        <r:given>#ctx.count > 0</r:given>
        <r:then>'action1'</r:then>
        <r:then>'action2'</r:then>
    </r:rule>

    <!--
        Rule with no given condition — the framework defaults to always-true.
    -->
    <r:rule name="NoConditionRule" description="Always passes (no condition)">
        <r:then>'executed'</r:then>
    </r:rule>

    <!--
        Rule that explicitly overrides the language on a single element.
    -->
    <r:rule name="LanguageOverrideRule" description="Tests per-element language override">
        <r:given language="el">#ctx.value == 42</r:given>
    </r:rule>

</beans>
